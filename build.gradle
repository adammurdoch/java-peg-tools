subprojects {
    apply plugin: 'java'
    apply plugin: 'groovy'

    repositories {
        jcenter()
    }

    dependencies {
        testImplementation 'org.spockframework:spock-core:1.1-groovy-2.4'
    }
}